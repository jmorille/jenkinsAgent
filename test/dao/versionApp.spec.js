const expect = require('chai').expect

const versionDAO = require('../../src/dao/versionAppDAO')

describe('Version App reader', function () {

    it('saves the content', function * () {
        const url = 'google.com'
    })

})
